import React from "react";

const ContactTabContent: React.FC = () => {
  return (
    <div>
      <h2>Contact on Social</h2>
      {/** TODO: add email, github, linkedin etc and its links */}
    </div>
  );
};

export default ContactTabContent;
