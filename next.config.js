/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    // Specifies the ESLint configuration to use for Next.js
    ignoreDuringBuilds: true,
  },
};

module.exports = nextConfig;
